#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
/*
//구조체
struct A {
	int i;
	int j;
};

int main() {

	struct A k; // struct A 데이터형을 갖는 변수 k
	k.i = 10;
	k.j = 20;

	return 0;
}

//추상 데이터 타입(ADT)
//class 사용(객체 지향)
//데이터를 가지고 어떠한 기능을 하는지도 구상하는 것
int main() {

	struct A k; // struct A 데이터형을 갖는 변수 k
	k.i = 10;
	k.j = 20;

	//안에 데이터를 활용
	if (k.i > k.j) {
		printf("i is bigger");
	}
	else {
		printf("j is bigger or same");
	}

	return 0;
}

//펙토리얼 계산 프로그래밍
int factorial(int n) {
	int result = 1;
	for (int i = 0; i <= n; i++) {
		result *= i;
	}
	return result;
}

//recursion을 이용한 factorial 계산(탈출 조건 무조건 필요)
int factorial_recursion_edition(int n) {
	//N * (N - 1)!
	//탈출조건
	if (n <= 1)return 1;
	return n * factorial_recursion_edition(n - 1); //생김새는 같지만 다른 집을 가지는 다른 것!(독립적)(집을 여러개 만들어 주기에 메모리 차지 높음,부가적 정보 또한 많음)
}

int main() {

	int m = factorial_recursion_edition(4);

	return 0;
}
*/

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main() {

	 

	return 0;
}